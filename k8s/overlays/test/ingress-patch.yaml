apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resume-analyzer
spec:
  rules:
    - host: test.resume-analyzer.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: resume-analyzer
                port:
                  number: 80
