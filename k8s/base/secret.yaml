# Placeholder — DO NOT COMMIT REAL VALUES.
# In real clusters, create via:
#   kubectl -n <ns> create secret generic resume-analyzer-secrets \
#     --from-literal=VITE_MISTRAL_API_KEY=your_key_here \
#     --from-literal=VITE_SUPABASE_SERVICE_KEY=your-service-key-here
# Or use SealedSecrets / ExternalSecrets / SOPS.
apiVersion: v1
kind: Secret
metadata:
  name: resume-analyzer-secrets
type: Opaque
stringData:
  MISTRAL_API_KEY: ""
  SUPABASE_SERVICE_ROLE_KEY: ""
